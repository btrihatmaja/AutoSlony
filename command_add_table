
	
	#--
	# define the namespace the replication system uses in our example it is
	# slony_example
	#--

	cluster name = myrep;

	#--
	# admin conninfo's are used by slonik to connect to the nodes one for each
	# node on each side of the cluster, the syntax is that of PQconnectdb in
	# the C-API
	# --
	node 1 admin conninfo = 'dbname=master host=localhost user=postgres password=postgres';
	node 2 admin conninfo = 'dbname=slave host=localhost user=postgres password=postgres';
	create set (id=2, origin=1, comment='a second replication set');
	set add sequence (set id=2, origin=1, id=3, fully qualified name = 'public.reptable3_id_seq', comment ='some new sequence');
	subscribe set(id=2, provider=1,receiver=2);
	merge set(id=1, add id=2,origin=1);
	